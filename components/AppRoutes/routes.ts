const user = {
   login: '/user/login',
   signup: '/user/signup',
   logout: '/user/logout',
}

const pages = {
   home: '/home',
}

const errors = {
   fiveHundred: '/errors/500',
   notFound: '/errors/404',
}

const ROUTES = {
   user,
   pages,
   errors,
}

export default ROUTES
