import { createGlobalStyle } from 'styled-components'

// Global style done using styled-components will provide app wide styles.
// As a styled component it accepts props, including the theme prop from the ThemeProvider
// Use to apply global styling
const GlobalStyle = createGlobalStyle``

export default GlobalStyle
